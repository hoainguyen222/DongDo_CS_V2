# 🚀 Đông Đô CS Core V2 — Enterprise Architecture

Hệ thống Chăm Sóc Khách Hàng Thông Minh & Quản Trị Tri Thức Đông Đô Partners phiên bản 2.0.

Được tái cấu trúc hoàn toàn từ Python/JS sang **Golang (Gin + SQLC + Goose) + Next.js (TypeScript)** theo chuẩn **Clean Architecture**, tích hợp **Redis Streams Event Bus**, **Qdrant Vector DB** và **WebRTC Voice Calling**.

---

## 🛠️ Công Nghệ Sử Dụng

| Thành phần | Công nghệ | Chi tiết |
|---|---|---|
| **Backend API** | Go 1.26 / Gin / pgx v5 / **SQLC** | Clean Architecture, type-safe SQL, Zero Allocations |
| **Migrations** | **Goose v3** | Embedded trong binary, Up/Down trong 1 file SQL |
| **Frontend** | Next.js 14 / TypeScript / TailwindCSS | Dark Glassmorphism, Micro-animations |
| **Realtime** | WebSocket Hub + Goroutines | Auto-reconnect, Typing Indicator, Heartbeat Ping/Pong |
| **Event Bus** | Redis Streams | Consumer Groups (`ws_group`, `ai_group`, `db_group`), XACK, XAUTOCLAIM, DLQ |
| **Redis State** | Redis Key-Value & Sets | Distributed Lock, Unread Counter, Typing State (TTL 3s) |
| **Vector DB** | Qdrant (gRPC) | Vector search cosine, metadata filtering, continuous learning |
| **LLM Engine** | Claude Haiku 4.5 / Sonnet / Opus | RAG Pipeline tiếng Việt, Zero hallucination |
| **Voice Calling** | WebRTC (P2P Audio) | Gọi điện thoại 2 chiều qua trình duyệt, STUN server, ghi âm |
| **Database** | PostgreSQL 16 | Source of Truth, Batch Upsert (pgx.Batch), Idempotent |
| **Observability** | Prometheus + Grafana + pprof | `/metrics`, `/debug/pprof`, 5 dashboards, alert rules — see `monitoring/README.md` |

---

## 📂 Cấu Trúc Thư Mục

```text
DongDo_CS_V2/
├── cmd/
│   ├── server/main.go          # Entry point Golang server
│   └── ingest/main.go          # CLI tool nạp tài liệu vào Qdrant
├── internal/
│   ├── config/config.go        # Configuration loader
│   ├── domain/                 # Entity & Interface (Pure Go)
│   ├── usecase/               # Business Logic (Auth, Chat, Case, RAG, Voice, Learning)
│   ├── repository/
│   │   ├── postgres/          # ✅ Repository implementation (wraps sqlc queries)
│   │   │   ├── db.go          # pgxpool + goose migrations + seed
│   │   │   ├── embed.go       # //go:embed migrations/
│   │   │   ├── convert.go     # pgtype ↔ domain helpers
│   │   │   ├── user_repo.go, session_repo.go, guest_repo.go, ...
│   │   │   └── migrations/    # ✅ EMBEDDED goose migrations
│   │   └── sqlcdb/            # ✅ GENERATED by sqlc — DO NOT EDIT
│   │       ├── auth/          # Users, Sessions
│   │       ├── chat/          # Guests, Messages, Cases
│   │       ├── learning/      # LearningQueue
│   │       ├── voice/         # VoiceCalls
│   │       ├── settings/      # SystemSettings
│   │       ├── analytics/     # Stats
│   │       └── partner/       # Dashboard, Config, Reports
│   ├── delivery/
│   │   ├── http/              # REST API Handlers & Middleware (Gin)
│   │   └── ws/               # WebSocket Hub, Client & Signaling
│   ├── worker/                # Background Workers (WS, AI, DB Batch, Retry/DLQ)
│   └── infra/                # Redis, Qdrant, Claude API, Embedding
├── pkg/
│   ├── graceful/              # Graceful shutdown manager
│   └── security/              # PBKDF2 Password hashing
├── db/
│   ├── migrations/            # ✅ Goose migrations (migrator CLI reference)
│   └── queries/              # ✅ SQLC input — viết SQL, generate code
│       ├── auth/             # Users, Sessions
│       ├── chat/             # Guests, Messages, Cases
│       ├── learning/
│       ├── voice/
│       ├── settings/
│       ├── analytics/
│       └── partner/
├── sqlc.yaml                 # ✅ sqlc config (7 packages)
├── tools/bin/                # sqlc, goose CLI binaries
├── web/                     # Next.js Frontend
├── docker-compose.yml
├── Dockerfile
├── monitoring/              # 📊 Observability stack (Prometheus, Grafana, exporters)
│   ├── docker-compose.monitoring.yml
│   ├── prometheus/          # scrape config + alert rules
│   ├── alertmanager/
│   ├── grafana/             # provisioning + dashboards
│   └── README.md            # operational runbook
└── README.md
```

---

## 🔧 Cài Đặt Công Cụ (Dev Setup)

Cần 2 binary: **sqlc** (generate code) và **goose** (migrations).

```bash
# Tải sqlc v1.27.0 (Darwin ARM64)
curl -sSL https://github.com/sqlc-dev/sqlc/releases/download/v1.27.0/sqlc_1.27.0_darwin_arm64.zip -o /tmp/sqlc.zip
unzip -o /tmp/sqlc.zip sqlc -d tools/bin/
chmod +x tools/bin/sqlc

# Tải goose v3.22.1 (Darwin ARM64)
curl -sSL https://github.com/pressly/goose/releases/download/v3.22.1/goose_darwin_arm64 -o tools/bin/goose
chmod +x tools/bin/goose

# Verify
./tools/bin/sqlc version   # → v1.27.0
./tools/bin/goose --help  # → goose migration tool
```

---

## 🔄 Workflow Dev: Thêm / Sửa Database Query

### Bước 1: Viết SQL trong `db/queries/`

```sql
-- db/queries/auth/users.sql
-- name: GetUserByUsername :one
SELECT id, username, password_hash, salt, full_name, role, is_active, created_at
FROM users
WHERE LOWER(username) = LOWER($1);
```

### Bước 2: Chạy sqlc generate

```bash
make sqlc-gen
# hoặc: ./tools/bin/sqlc generate
```

**Output:** `internal/repository/sqlcdb/auth/users.sql.go` — tự động được tạo với:
- Struct `GetUserByUsernameParams` (type-safe params)
- Method `GetUserByUsername(ctx, params) (User, error)`

### Bước 3: Viết / cập nhật Repository adapter

```go
// internal/repository/postgres/user_repo.go
func (r *UserRepo) GetByUsername(ctx context.Context, username string) (*domain.User, error) {
    row, err := r.db.Auth.GetUserByUsername(ctx, authdb.GetUserByUsernameParams{
        Username: username,
    })
    if err != nil {
        if errors.Is(err, pgx.ErrNoRows) {
            return nil, nil
        }
        return nil, err
    }
    return &domain.User{
        ID: row.ID, Username: row.Username, Role: row.Role, ...
    }, nil
}
```

### Bước 4: Verify build

```bash
go build ./...
```

---

## 🗄️ Workflow Migrations (Goose)

### Cấu trúc migration file

```sql
-- db/migrations/00004_new_feature.sql
-- +goose Up
-- +goose StatementBegin
CREATE TABLE new_table (...);
CREATE INDEX ON new_table(...);
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DROP TABLE new_table;
-- +goose StatementEnd
```

### Tạo migration mới

```bash
# Cách 1: Dùng goose CLI
./tools/bin/goose create add_new_feature sql

# Cách 2: Tạo tay
touch db/migrations/00004_add_new_feature.sql
# Viết nội dung với -- +goose Up / Down

# Copy vào embedded folder để binary nhận
cp db/migrations/00004_add_new_feature.sql internal/repository/postgres/migrations/
```

### Chạy migrations

```bash
# Tự động (server startup) — goose chạy trong NewDB()
make up

# Thủ công bằng goose CLI
make migrate-up
make migrate-down
make migrate-status

# Dùng Make shortcut
make migrate-create name=add_tags_column
```

### Reset hoàn toàn (⚠️ XÓA HẾT DATA)

```bash
make down
docker volume rm dongdo_cs_v2_pgdata
make up          # goose tự chạy migrations
```

---

## 🚀 Hướng Dẫn Khởi Chạy

### Cách 1: Docker Compose (Khuyên dùng)

```bash
# 1. Cấu hình API Key
cp .env.example .env
# Chỉnh sửa .env: điền ANTHROPIC_API_KEY

# 2. Build & Up toàn bộ hạ tầng
make up
# Docker Compose khởi: Postgres (port 5433) + Redis + Qdrant + Server

# 3. Nạp tài liệu vào Qdrant (sau khi server đã chạy)
docker compose exec server /app/ingest
```

### Cách 2: Chạy trực tiếp trên máy (Local Dev)

**Yêu cầu:** Postgres 16 (port 5433), Redis, Qdrant đang chạy.

```bash
# 1. Chạy migrations (goose tự chạy khi server start,
#    hoặc chạy thủ công)
./tools/bin/goose -dir internal/repository/postgres/migrations \
    postgres "postgres://postgres:postgrespassword@localhost:5433/dongdo_cs?sslmode=disable" up

# 2. Build & chạy server
go build -o server_new ./cmd/server
./server_new

# 3. Build & chạy ingest (nạp tài liệu Qdrant)
go build -o ingest_new ./cmd/ingest
./ingest_new

# 4. Frontend (terminal khác)
cd web && npm install && npm run dev
```

### Full Dev Workflow

```bash
# Generate sqlc → Build → Run
make dev

# Hoặc từng bước:
make sqlc-gen    # 1. Generate code
make build       # 2. Build binary
make migrate-up  # 3. Run migrations
./server_new     # 4. Chạy server
```

---

## 🧪 Hướng Dẫn Test Load / Stress Test

Sau khi server đã chạy (Docker hoặc local), chạy batch test từ `tests/automation/`:

```bash
cd tests/automation && npm install
```

### Các script test có sẵn

| Lệnh | Mô tả | Tải |
|---|---|---|
| `npm run batch:smoke` | 2 agents, 2 guests, 2 msg | Nhẹ |
| `npm run batch:light` | 5 agents, 5 guests, 5 msg | Nhẹ |
| `npm run batch:medium` | 10 agents, 10 guests, 8 msg | Trung bình |
| `npm run batch:heavy` | 15 agents, 20 guests, 10 msg | Nặng |
| `npm run batch:stress` | 20 agents, 50 guests, 5 msg | Cực nặng |

### Test AI response (ko cần agent)

```bash
# Test AI chịu tải - 10 guests đồng thời, mỗi guest 5 messages
ts-node batch-runner.ts --scenario ai_load_test --test chat --agents 3 --parallel
```

### Test CS Team (chuyển sang human)

```bash
# Test team agent nhận case từ 20 guests
ts-node batch-runner.ts --scenario cs_team_load_test --test mixed --agents 5 --parallel
```

### Test tùy chỉnh

```bash
# Custom: 5 agents, chạy chat + cs test, parallel
ts-node batch-runner.ts --agents 5 --test mixed --parallel

# Sequential (từng agent chạy xong mới chạy tiếp)
ts-node batch-runner.ts --agents 5 --test chat --sequential

# Scenario cụ thể
ts-node batch-runner.ts --scenario medium_load --agents 5 --parallel
```

### Xem metrics

Sau mỗi batch test sẽ hiển thị:
- **Total agents / success / failed**
- **Avg / min / max duration**
- **Total messages gửi / nhận**
- **AI response time** (nếu dùng chat test)

---

## 🔑 Tài Khoản Mặc Định

| Tài khoản | Mật khẩu | Quyền hạn |
|---|---|---|
| `admin` | `DongDo@2026` | Quản trị viên hệ thống |
| `cskh01` - `cskh05` | `DongDo@123` | Chuyên viên CSKH |
| **Khách hàng** | *Không cần mật khẩu* | Chỉ cần nhập Tên + SĐT (tùy chọn) |

---

## 📊 Observability / Monitoring

The system ships with a production-ready observability stack: Prometheus,
Grafana, pprof and exporters for PostgreSQL, Redis and the host. It lives
in `monitoring/` and is enabled via a separate Docker Compose profile so
it stays opt-in.

### Chạy nhanh

```bash
# 1. Chạy app stack (Postgres, Redis, Qdrant, Server).
make up

# 2. Chạy monitoring stack (Prometheus, Grafana, exporters).
make monitoring-up

# 3. Mở các UI (chờ ~15s để Prometheus scrape xong):
#    Grafana       → http://localhost:3050  (admin / admin — đổi ngay!)
#    Prometheus    → http://localhost:9091  (đổi từ 9090 sang 9091 để tránh trùng với /metrics của Go server)
#    Alertmanager  → http://localhost:9093
```

### Các lệnh Makefile thường dùng

```bash
make up                  # Start app stack
make monitoring-up       # Start monitoring stack (phải chạy sau `make up`)
make monitoring-down     # Stop monitoring stack
make monitoring-logs     # Tail logs của monitoring
make monitoring-status   # Xem containers nào đang chạy
make metrics             # Hit /metrics của Go server
make health              # Probe /health endpoint
make pprof-cpu           # Capture 30s CPU profile (interactive)
make pprof-heap          # Heap snapshot
make pprof-goroutine     # Goroutine dump
make down                # Stop app stack
```

### Endpoints quan trọng

| Service          | URL                                    | Bảo mật               |
|------------------|----------------------------------------|-----------------------|
| App API          | `http://localhost:8080`                | Public                |
| App `/metrics`   | `http://localhost:9090/metrics`        | Loopback only         |
| App `/debug/pprof` | `http://localhost:6060/debug/pprof/` | Loopback only         |
| Grafana          | `http://localhost:3050`                | admin / admin (đổi!); **3050** để không trùng với `npm run dev` |
| Prometheus       | `http://localhost:9091`                | Expose nội bộ (loopback only); 9091 để tránh xung đột với `/metrics` 9090 của Go server |
| Alertmanager     | `http://localhost:9093`                | Expose nội bộ        |

> ⚠️ **`/metrics` và `/debug/pprof` mặc định bind `127.0.0.1`** — an toàn cho production. Muốn truy cập từ xa, dùng `ssh -L 9090:127.0.0.1:9090 user@host` rồi mở `http://localhost:9090`.

Five pre-built dashboards (auto-load vào Grafana):

- **Infrastructure** — host CPU / memory / disk / network / load
- **Golang API** — request rate, error rate, P50/P95/P99, goroutines, heap, GC
- **Chat / WebSocket** — active connections, messages/sec, agent states, waiting customers
- **Database** — connections, slow queries (`pg_stat_statements`), locks
- **Redis** — memory, commands, streams, pool stats

Full operational runbook — debug CPU, memory leak, slow SQL, WS lag, alerts — xem **[`monitoring/README.md`](monitoring/README.md)**.

---

## 🧩 Tham Chiếu Nhanh

| Thao tác | Lệnh |
|---|---|
| Generate sqlc code | `make sqlc-gen` hoặc `./tools/bin/sqlc generate` |
| Tạo migration mới | `make migrate-create name=ten_migration` |
| Run migrations | `make migrate-up` |
| Rollback migration | `make migrate-down` |
| Kiểm tra migration | `make migrate-status` |
| Build binary | `make build` |
| Chạy dev (gen+build+run) | `make dev` |
| Full reset (xóa data) | `make down && docker volume rm dongdo_cs_v2_pgdata && make up` |

---

## 📌 Lưu Ý Quan Trọng

1. **KHÔNG edit** `internal/repository/sqlcdb/` — đây là code được **sqlc generate tự động**. Mọi thay đổi SQL → chỉnh sửa file trong `db/queries/` rồi chạy `make sqlc-gen`.

2. **KHÔNG edit** `internal/repository/postgres/migrations/` — đây là bản **embedded** của `db/migrations/`. Copy file mới vào đây sau khi tạo migration.

3. **Goose migrations** tự động chạy khi `NewDB()` được gọi (trong `cmd/server/main.go`). Không cần chạy thủ công trừ khi muốn migrate trước khi start server.

4. **Volume cũ (golang-migrate):** Nếu đã có volume chạy từ trước với golang-migrate, cần wipe volume để bắt đầu với goose:
   ```bash
   docker volume rm dongdo_cs_v2_pgdata
   ```
