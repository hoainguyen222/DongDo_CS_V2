;/etc/asterisk/cel.conf - Channel Event Logging
;=================================================================================
; CEL is finer-grained than CDR (event-based).  Used by backend for analytics.
;=================================================================================

[general]
enable=yes
dateformat=%F %T

[manager]
enabled=yes

[csv]
enabled=yes