;/etc/asterisk/cel_manager.conf - Push CEL events via AMI
;=================================================================================
; Send CEL events through AMI as separate event type; backend reads them.
;=================================================================================

[general]
enabled=yes
; Date format used in event payloads
dateformat=%F %T