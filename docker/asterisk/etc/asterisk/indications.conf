;/etc/asterisk/indications.conf - Country tone definitions
;=================================================================================

[general]
country=us     ; Default to US tones; runtime config driven by env
; Override with indication.conf?  Lower priority goes here:
;setcountry=no