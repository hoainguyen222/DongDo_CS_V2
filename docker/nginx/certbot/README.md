# Placeholder — real Let's Encrypt certificates land here when you run
# `make nginx-ssl-init`. This directory is bind-mounted into the nginx
# container at /etc/letsencrypt (read-only) and into the certbot sidecar
# at /etc/letsencrypt (read-write).
#
# This file is git-ignored so an empty directory is safe to commit.
